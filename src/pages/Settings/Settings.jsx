import SettingsContent from "./SettingsContent";
import Tab from "../../components/Tab/Tab";

const Settings = () => {
    return <Tab name="Settings" content={<SettingsContent />} />;
};

export default Settings;
